<h1><%=@person.name %> </h1>
<%= image_tag attachment_url(@person, :image, :fill, 200, 200, format: "jpg") , class:"img-responsive col-md-4" %>
<%= form_for(@classroom , url:[@person ,@classroom],html:{class:"form"}) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>

    </div>
  <% end %>
   <h2>Associar a Curso </h2>




 </div >
  <div class ="field">
   <%= f.label :course %><br>
   <%= f.collection_select :course_id, @courses, :id, :name, {:multiple => true}, class:"form-control" %>
 </div>
  <div class ="field">
  <%= f.label :Data %><br>
  <%= f.text_field :entry_at, class:"form-control" , id:"datetime"%>
</div>
  <p></p>
  <div class="actions">
    <%= f.submit class: "submit", value:"matricular" %>
  </div>
<% end %>
